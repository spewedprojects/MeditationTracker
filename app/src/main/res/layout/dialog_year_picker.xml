<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/db_year_picker"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:clickable="true"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/dialogbox_years"
        android:layout_width="0dp"
        app:layout_constraintWidth_max="320dp"
        android:layout_height="280dp"
        android:layout_marginVertical="36dp"
        android:layout_marginHorizontal="70dp"
        app:cardCornerRadius="12dp"
        app:strokeWidth="0dp"
        app:strokeColor="@color/transparent"
        app:cardElevation="16dp"
        app:cardBackgroundColor="?attr/colorPrimaryVariant"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!--TextView
            android:id="@+id/list_years"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="0dp"
            android:text="2026"
            android:gravity="center"
            android:textSize="65sp"
            android:fontFamily="@font/atkinsonhyperlegiblenext_medium"
            android:clickable="true"
            android:focusable="true"/-->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_years"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:overScrollMode="never"
            android:requiresFadingEdge="vertical"
            android:fadingEdgeLength="40dp"
            android:clipToPadding="false"/>

    </com.google.android.material.card.MaterialCardView>

    <androidx.constraintlayout.widget.Barrier
        android:id="@+id/barrier_card_end"
        android:layout_height="match_parent"
        android:layout_width="wrap_content"
        app:barrierDirection="end"
        app:constraint_referenced_ids="dialogbox_years"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnselect_Year"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select"
        android:textColor="?attr/colorPrimary"
        android:fontFamily="@font/atkinsonhyperlegiblenext_medium"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="16dp"
        android:layout_marginEnd="0dp"
        app:cornerRadius="12dp"
        app:elevation="12dp"
        android:backgroundTint="?attr/colorOnPrimary"
        app:layout_constraintTop_toBottomOf="@id/dialogbox_years"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/barrier_card_end"
        app:layout_constraintHorizontal_bias="1"/>

</androidx.constraintlayout.widget.ConstraintLayout>